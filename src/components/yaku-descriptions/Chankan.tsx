import React from 'react';

const Chankan = () => (
  <p>
    Chankan is scored when a player calls their winning tile from another player
    who declares an open kan (quad) of that tile. In other words, if a player is
    in tenpai and waiting on a tile that another player uses to call a kan, the
    first player may then declare ron on that tile and win.
  </p>
);

export default Chankan;
